apiVersion: apps/v1
kind: Deployment
metadata:
  name: mqtt-controller-deployment # TODO rename deployment and service to match convention
  labels:
    app: mqtt-controller
spec:
  replicas: 1 # TODO make sure any controller can check for server and add client password
  selector:
    matchLabels:
      app: mqtt-controller
  template:
    metadata:
      labels:
        app: mqtt-controller
    spec:
      containers:
        - name: mqtt-controller-application
          image: d3lta12/mqtt-controller-application:public
          imagePullPolicy: Always
          ports:
            - containerPort: 10080
          env:
            - name: MONGOURI
              valueFrom:
                secretKeyRef:
                  name: mongo-uri
                  key: MONGOURI
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: "10080"
            - name: REDIS_ADDR
              value: "mqtt-server-cache-service"
